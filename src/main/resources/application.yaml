spring:
  application:
    name: WorkFlowLite

  datasource:
    url: jdbc:postgresql://localhost:5342/workflowlite
    password: password
    username: gusbom
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    #    show-sql: true
    open-in-view: true